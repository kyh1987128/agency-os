/* Phase 2에서 구글 시트 연동 구현 예정 */
// const API_URL = import.meta.env.VITE_API_URL;

// export const fetchProjects = () => fetch(`${API_URL}?action=projects`).then(r => r.json());
// export const fetchTasks    = () => fetch(`${API_URL}?action=tasks`).then(r => r.json());
// export const fetchHumans   = () => fetch(`${API_URL}?action=humans`).then(r => r.json());
// export const updateTask    = (id, data) => fetch(API_URL, { method: "PUT", body: JSON.stringify({ id, ...data }) }).then(r => r.json());
